flowchart TD
    A[开始] --> B{判断 n 是否为 1}
    B -->|是| C[返回 1]
    B -->|否| D[初始化 dp 数组]
    D --> E[设置 dp1 为 1]
    E --> F[设置 dp2 为 2]
    F --> G[循环 i 从 3 到 n]
    G --> H{是否 i <= n}
    H -->|是| I[计算 dpi = dpi - 1 + dpi - 2]
    I --> G
    H -->|否| J[返回 dpn]
